{
  "name": "board-gamer-2d",
  "description": "2d multiplayer html5 board game simulator",
  "version": "0.0.0",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git://github.com/thejoshwolfe/board-gamer-2d.git"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "scripts": {
    "build": "npm install --no-audit --no-fund && npm run build:client && npm run build:server",
    "build:client": "cd public && npm run build",
    "build:server": "cd lib && npm run build",
    "run": "cd lib && npm run run"
  }
}
